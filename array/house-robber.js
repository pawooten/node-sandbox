/**
 * @param {number[]} nums
 * @return {number}
 */
 var rob = function(nums) {
    // If nums.length is 2, we may select one house to rob only.
    // If nums.length is 3, we may select either two houses (0 and 2) or one house (1).
    // If nums.length is 4, we may select two houses (0+2, 0+3, 1+3).
    // If nums.length is 5, we may select either two houses or three.
};

const nums = [2,7,9,3,1];
const result = rob(nums);
console.log('!');