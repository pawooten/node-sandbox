/**
 * @param {string} s
 * @param {string} t
 * @return {character}
 */
 var findTheDifference = function(s, t) {
    // string is generated by randomly shuffling string s and then add one more letter at a radnom position.
    // return the letter that was added by t.
    let differentChar = null;
    const sChars = s.split(''), tChars = t.split('');

    for (let index = 0; index < sChars.length; index++) {

    }
    return differentChar;
};
const s = "a", t = "aa";
const result = findTheDifference(s, t);
console.log('!');