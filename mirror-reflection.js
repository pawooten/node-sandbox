/**
 * @param {number} p
 * @param {number} q
 * @return {number}
 */
 var mirrorReflection = function(p, q) {
    // p is the width of each wall in the square room.
    // q is the distance between the 0th receptor and where the laser ray emitted from the southwest corner meets the east wall.
    Math.sin
};

const p = 2, q = 1;
const result = mirrorReflection(p, q);
console.log('!');